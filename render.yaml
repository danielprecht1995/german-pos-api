# Docker build uses Python 3.11 — add spacy for full POS tagging
services:
  - type: web
    name: german-pos-api
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: SPACY_MODEL
        value: de_core_news_sm
